rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access is performed through Cloud Functions (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

