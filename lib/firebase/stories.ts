import type { Story } from '@/lib/types/story';

/**
 * Fetch all published stories from Firebase.
 * TODO: implement real Firebase query.
 */
export async function fetchPublishedStories(): Promise<Story[]> {
  return [];
}
